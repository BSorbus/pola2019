$('#through_events_attachment_file_preview_area').html('');
$('#through_events_attachment_file_preview_area').prepend('<div><%= image_tag @attachment.attached_file_url(:thumb), class: "img img-thumbnail" %></div');
$('#through_events_attachment_file_preview_area').prepend('<div><strong>Plik:</strong> <%= @attachment.fullname %>, <%= @attachment.file_size %></div');
