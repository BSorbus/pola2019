<% if @attachment.new_record? %>
  // alert("Error: <%= j @attachment.errors.full_messages.join(', ').html_safe %>");
  toastr['error']("<%= @attachment.errors.full_messages.join(', ').html_safe %>");
<% else %>
  $('#attachmenable-attachments').DataTable().draw();
  toastr['success']("<%= @attachment.attached_file.file.filename %>" + '<br>...pobieranie zakończone');
<% end %>
