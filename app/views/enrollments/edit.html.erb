<br>
<div class="container-fluid"> 
  <div class="row">
    <div class="col-sm-2">
      <div class="center colored-title"><%= fa_icon("edit", size: '3x') %></div>
    </div>
    <div class="col-sm-10">
      <fieldset class="my-fieldset">
        <legend class="my-fieldset colored-title"><%= enrollment_edit_legend(@enrollment) %></legend>
        <%= render 'form', enrollment: @enrollment %>
      </fieldset>
    </div>

  </div>

</div>
