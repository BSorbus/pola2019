<div id="tab_roles">
  <fieldset class="my-fieldset">
    <legend class="my-fieldset colored-title"><%= subtitle %>&nbsp;&nbsp;-&nbsp;&nbsp;<%= fa_icon("user-tag", text: t("pages.role.index.title")) %></legend>

    <div class="row">
      <div class="col-sm-12">
        <table id="user-roles-table" class="hover cell-border compact" width="100%" 
          data-source="<%= datatables_index_user_roles_path(user_id: @user.id, format: :json) %>">
          <thead>
            <tr>
              <th                  >ID</th>
              <th class="col-sm-3"><%= Role.human_attribute_name(:name) %></th>
              <th class="col-sm-7"><%= Role.human_attribute_name(:activities) %></th>
              <th class="col-sm-1" >Uprawnienie</th>
              <th class="col-sm-1" >Akcje</th>
            </tr>
          </thead>
          <tbody>
             <!--data from JSON -->
          </tbody>
        </table>
      </div> <!--/col-sm-12 -->
    </div> <!--/row -->

  </fieldset>
</div>