<% if @photo.new_record? %>
  alert("Failed to upload photo: <%= j @photo.errors.full_messages.join(', ').html_safe %>");
<% else %>
  $('#photoable-photos').DataTable().draw();
  toastr['success']("<%= @photo.attached_file.file.filename %>" + '<br>...pobieranie zakończone');
<% end %>
