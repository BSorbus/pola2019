<div id="tab_through_events_photos">
  <fieldset class="my-fieldset">
    <legend class="my-fieldset colored-title"><%= subtitle %>&nbsp;&nbsp;-&nbsp;&nbsp;<%= through_events_photo_index_legend %></legend>

    <div class="row">

      <div class="col-sm-12">
        <table id="through-events-photoable-photos" class="hover cell-border compact" width="100%" 
          data-source="<%= datatables_index_through_events_photos_path(for_parent_type: for_parent.class.to_s, for_parent_id: for_parent.id.to_s, format: :json) %>">
          <thead>
            <tr>
              <th>0 ID </th>
              <th><%= Photo.human_attribute_name(:miniature) %></th>
              <th><%= Photo.human_attribute_name(:photo_created_at) %></th>
              <th><%= Photo.human_attribute_name(:latitude) %></th>
              <th><%= Photo.human_attribute_name(:longitude) %></th>
              <th><%= Photo.human_attribute_name(:attached_file) %></th>
              <th><%= Photo.human_attribute_name(:photoable) %></th>
              <th><%= Photo.human_attribute_name(:note) %></th>
              <th><%= Photo.human_attribute_name(:user_id) %></th>
              <th><%= Photo.human_attribute_name(:updated_at) %></th>
              <th><%= Photo.human_attribute_name(:file_size) %></th>
              <th></th>
            </tr>
          </thead>
          <tbody>                    
            <!--data from JSON -->
          </tbody>
        </table>
      </div> <!--/col-sm-12 -->

    </div>

  </fieldset>
</div>

